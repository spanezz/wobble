cmake_minimum_required(VERSION 3.2)

include(FindDoxygen)
set(VERSION "1.0")
option(HAVE_TUT ON)
if (NOT WIN32)
    add_definitions(-DPOSIX)
endif()
set(CMAKE_CTEST_COMMAND wobble/wobble-test)
add_custom_target(check COMMAND ${CMAKE_CTEST_COMMAND})
add_subdirectory(wobble)
